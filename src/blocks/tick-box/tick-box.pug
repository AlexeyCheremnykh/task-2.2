mixin tick-box({checked = true, label = ""} = {})
    label(class=label ? "tick-box" : "tick-box tick-box_not-labeled")=label
        input.tick-box__auxiliary-checkbox(type="checkbox" checked=checked)
        .tick-box__checkmark
