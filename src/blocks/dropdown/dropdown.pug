mixin dropdown(options, mixClass = "")
    - mixClass = mixClass ? " " + mixClass : mixClass;
    select(class="dropdown" + mixClass)
        option(value="")
        each opt in options
            option(class="dropdown__option" value=opt)=opt

