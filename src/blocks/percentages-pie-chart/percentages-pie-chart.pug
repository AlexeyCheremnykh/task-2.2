include ../percentage/percentage.pug
include ../pie-chart/pie-chart.pug
include ../section-title/section-title.pug

mixin percentages-pie-chart
    section.percentages-pie-chart
        +section-title({title: "Percentages / Pie Chart", shrunk: true})
        .percentages-pie-chart__content
            .percentages-pie-chart__percentage
                +percentage({value: 0})
            .percentages-pie-chart__percentage
                +percentage({value: 38})
            +percentage({value: 62})
            +percentage({value: 89})
            +pie-chart({values: [6, 26, 12, 30]})
