mixin news({title, date, text, mixClass = ""})
    - mixClass = mixClass ? " " + mixClass : mixClass;
    div(class="news" + mixClass)
        .news__image
        .news__text
            .news__title #{title}
            .news__date #{date}
            .news__content #{text}
