mixin news(options)
    - var title = options.title
    - var date = options.date
    - var text = options.text
    .news
        img(class="news__image" src="news/img/news-sample.jpg" alt="news image")
        .news__text
            .news__title #{title}
            .news__date #{date}
            .news__content #{text}
