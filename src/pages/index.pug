include ../blocks/ui-kit/ui-kit.pug

doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        meta(name="viewport" content="initial-scale=1.0, width=device-width")
        meta(name="description" content="Flat UI is a UI kit that contains arrow buttons, standard buttons, search form, sliders, pie-chart and other cool stuff.")
        meta(name="keywords" content="ui kit, user interface, ui element, web design")
        link(rel="apple-touch-icon" sizes="180x180" href="favicons/apple-touch-icon.png")
        link(rel="icon" type="image/png" sizes="32x32" href="favicons/favicon-32x32.png")
        link(rel="icon" type="image/png" sizes="16x16" href="favicons/favicon-16x16.png")
        link(rel="manifest" href="favicons/site.webmanifest")
        link(rel="mask-icon" href="favicons/safari-pinned-tab.svg" color="#5bbad5")
        link(rel="shortcut icon" href="favicons/favicon.ico")
        meta(name="msapplication-TileColor" content="#da532c")
        meta(name="msapplication-config" content="/favicons/browserconfig.xml")
        meta(name="theme-color" content="#ffffff")
        title Flat UI
    body
        - const data = require("./block-data.json");
        +ui-kit(data)
